#include<bits/stdc++.h>
#define pb push_back
#define mp make_pair
#define all(v) v.begin(),v.end()
#define ff first
#define ss second
typedef long long ll;
using namespace std;
const ll mod=1000000007;
string s0="What are you doing while sending \"What are you doing at the end of the world? Are you busy? Will you save us?\"? Are you busy? Will you send \"What are you doing at the end of the world? Are you busy? Will you save us?\"?";
string s1="What are you doing while sending \"";
string s2="\"? Are you busy? Will you send \"";
string s3="\"?";
char calc(ll n,ll k,ll s0,ll s1,ll s2,ll s3)
{
	for(ll j=1;j<=4;j++)
	{
		if(k>S1)
		{
			k=k-n;
			calc();
		}
		
	} 
}
int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	ll S0=s0.size(),S1=s1.size(),S2=s2.size(),S3=s3.size();
	for(ll i=n;i>=0;)
	{
		for(ll j=1;j<=4;j++)
		{
			if(k>S1)
			{
				k=k-n;
				break;
			}

		}
	}
	return 0;
}